// PostCSS configuration for Tailwind CSS v4
// Uses the official combined plugin that includes Tailwind + nesting + autoprefixer
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};


